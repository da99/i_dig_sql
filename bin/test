#!/usr/bin/env bash
# -*- bash -*-
#
#
x="$1"
set -u -e -o pipefail


files="$(echo specs/*-$x.rb)"
if [[ -f "$files" ]]; then
  shift
else
  files="$(echo specs/*-*.rb)"
fi



bundle exec bacon -rpry -ri_dig_sql specs/helpers.rb $files "$@"


